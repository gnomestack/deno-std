/**
 * The `gs_std` module provides an extended standard library for deno, the
 * main entry point for gs_std pulls in modules for scripting.
 *
 * @module gs_std
 */
export * from "./scripting/mod.ts";
